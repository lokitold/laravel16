h1= $title
each $item, $key in $items
    p Item at #{$key} is #{$item}!

mixin btn(label)
    a.btn(class='btn-'.$class, id=$id)= $label
+btn.default('Default Button')
+btn.primary('Primary Button')
+btn.info('Info Button')
+btn.danger('Danger Button')